# 五子棋（Gomoku）开发文档

> 版本：v1.0 ｜ 状态：设计稿 ｜ 适用对象：服务端 / 客户端开发、测试、运维

---

## 1. 项目概述

### 1.1 定位

一款基于 Go 服务端的双人五子棋在线对战游戏，支持 **WebSocket / TCP** 接入，提供匹配对战、限时下棋、掉线 AI 接管、战绩与排行榜等功能。

### 1.2 核心特性

| 特性 | 说明 |
| --- | --- |
| 双人对战 | 1v1 匹配对战，15×15 棋盘，黑方先手，无禁手（v1，可配置扩展） |
| 限时下棋 | 每步限时（默认 30s）+ 每方总时限（默认 10min），超时判负，服务器权威计时 |
| 掉线 AI 接管 | 玩家掉线/离开后进入宽限期（默认 10s），逾期由 AI 代替其继续对局，对局不中断 |
| 在线状态/房间缓存 | Redis 缓存在线状态、房间信息、匹配队列、排行榜 |
| 持久化 | MySQL 存储账号与对局记录（远程库），成绩变更异步落库 |
| 消息协议 | Protobuf 二进制协议，前后端共用一份 `.proto` 定义（联调期可切 JSON 序列化，见 §7.5） |

### 1.3 术语表

| 术语 | 含义 |
| --- | --- |
| gate | Pitaya frontend 服务器，负责客户端连接接入、鉴权、session 管理 |
| room | Pitaya backend 服务器，承载匹配与对局逻辑（按房间分片，可多开） |
| rank | Pitaya backend 服务器，排行榜查询 |
| seat | 座位，0=黑方（先手），1=白方 |
| AI 接管 | 掉线玩家的落子权由 AI 代为行使，胜负结果仍归属该玩家 |

---

## 2. 技术栈与环境

| 项 | 选型 | 说明 |
| --- | --- | --- |
| 语言 | Go 1.25+ | `go.mod` 声明 `go 1.25.0`（Pitaya v2.11.24 要求） |
| 服务器框架 | [Pitaya](https://github.com/topfreegames/pitaya)（`github.com/topfreegames/pitaya/v2` v2.11.24） | 支持 WebSocket/TCP，frontend/backend 集群模型，内置 RPC 与序列化扩展 |
| MySQL | 远程库 `47.97.42.179:3306` | 持久化账号、角色数据、对局记录；驱动 `github.com/go-sql-driver/mysql` |
| Redis | 本地 `127.0.0.1:6379` | 在线状态、房间信息、匹配队列、排行榜缓存；驱动 `github.com/redis/go-redis/v9` |
| 协议 | Protobuf（proto3） | `google.golang.org/protobuf`，Pitaya 使用 `serialize.ProtobufSerializer`；联调期可用 JSON 序列化器快速打通（见 §7.5） |
| 客户端 | starx JS 官方客户端（[starx-client-websocket](https://github.com/nano-ecosystem/starx-client-websocket)，MIT） | 浏览器 WebSocket 客户端（starx 为 Pitaya 前身，协议兼容），已随仓库落地 `web/lib/`；默认 JSON 序列化，可注入 protobufjs 支持 protobuf（见 §7） |

> 环境说明（当前工作区）：
> - MySQL 连接串：`mysql://root:8A35Nn3BkrCpny65@47.97.42.179:3306`（库名建议 `gomoku`，见 `docs/schema.sql`）
> - Redis：本地默认 `127.0.0.1:6379`（可用 Docker 启动，见 §11）
>
> ⚠️ 安全提醒：上述 root 密码已明文出现在配置中，仅限开发环境使用。生产环境必须通过环境变量/密钥管理注入，并为应用创建最小权限账号（`SELECT/INSERT/UPDATE/DELETE`）。

---

## 3. 系统架构

### 3.1 总体架构

```
                    ┌─────────────────────────────┐
 客户端(WebSocket/TCP)│         Pitaya Cluster       │
 ──────────────────►│  gate (frontend) × N        │  连接接入、鉴权、session
   手机 / Web / PC   │  ├─ auth 组件               │
                    │  └─ session 路由            │
                    └─────────────┬───────────────┘
                                  │ RPC (直连 / NATS)
                    ┌─────────────┴───────────────┐
                    │  room (backend) × N         │  匹配 + 对局（按房间分片）
                    │  ├─ match 组件              │
                    │  ├─ room 组件（计时/裁决）   │
                    │  └─ ai 组件（AI 接管）       │
                    ├─────────────────────────────┤
                    │  rank (backend)             │  排行榜
                    └─────────────┬───────────────┘
                                  │
                    ┌─────────────┴───────────────┐
                    │        数据层                │
                    │  MySQL(远程): user/stats/record│
                    │  Redis(本地): online/room/queue/rank│
                    └─────────────────────────────┘
```

### 3.2 服务划分与职责

| 服务类型（Pitaya `server.type`） | 角色 | 职责 |
| --- | --- | --- |
| `gate` | frontend | WebSocket/TCP 接入；`auth.login` 登录鉴权；维护 uid ↔ session 绑定；按房间路由到对应 room |
| `room` | backend | 匹配撮合、创建/销毁房间、落子校验、胜负裁决、限时计时、掉线检测、AI 接管、重连恢复、对局结算 |
| `rank` | backend | 排行榜分页查询（读 Redis 缓存，缺失时回源 MySQL） |

- **单机开发模式**：Pitaya 以单进程运行（`app.Configure(false, ...)`，不依赖 NATS/etcd），三种类型组件共存于一个进程。
- **集群生产模式**：`gate` 可水平扩展；`room` 按房间分片多开（每房间绑定一个具体 server，通过 Redis 记录 `room:{id}.server_id` 供重连定向）；`gate → room` 走 NATS RPC。

### 3.3 部署拓扑（生产建议）

```
LVS/Nginx (wss 443)
   └── gate × 2~4（无状态，水平扩展）
          └── room × 2~N（按房间分片，动态扩缩）
          └── rank × 1~2
NATS cluster（RPC 总线，可选单节点）
MySQL（远程，主从）
Redis（本地实例，主从/哨兵可选）
```

### 3.4 技术选型要点

- **为什么用 Pitaya**：内置 WebSocket/TCP 支持、frontend/backend 消息路由与 session 管理、protobuf 序列化扩展点、集群 RPC，符合棋牌类服务器典型形态，避免自研网络层。
- **为什么房间状态双写内存 + Redis**：对局热路径（落子、计时）在内存中进行保证性能；Redis 保存房间摘要与走子记录，用于掉线重连时快速恢复与跨进程路由。

---

## 4. 项目目录结构

```
FIVE/
├── go.mod                     # module five；go 1.25（Pitaya v2.11.24 要求）
├── cmd/
│   └── server/
│       └── main.go            # 启动入口，按配置组装组件
├── config/
│   └── config.yaml            # 环境配置（见 §10）
├── proto/
│   └── gomoku.proto           # 协议源文件（与 docs/gomoku.proto 同步）
├── internal/
│   ├── app/
│   │   ├── auth/              # gate：登录/鉴权组件
│   │   ├── match/             # room：匹配组件（Redis 队列撮合）
│   │   ├── room/              # room：对局组件（状态机、计时、裁决、重连）
│   │   └── rank/              # rank：排行榜组件
│   ├── service/
│   │   ├── room_mgr.go        # 房间管理器（内存房间表 + Redis 镜像）
│   │   ├── settle.go          # 对局结算（写库、更新排行、发推送）
│   │   └── elo.go             # ELO 积分计算
│   ├── ai/
│   │   ├── engine.go          # AI 入口（按难度分发）
│   │   ├── score.go           # 棋型评分表与启发式
│   │   └── search.go          # α-β 剪枝（可选，难度档）
│   ├── model/
│   │   ├── mysql.go           # 数据库连接与 DAO
│   │   ├── redis.go           # Redis 连接与缓存读写
│   │   └── schema.go          # 领域模型（User/GameRecord/Stats…）
│   ├── protocol/              # protobuf 生成代码（gomoku/gomoku.pb.go，正式路径备用）
│   └── common/
│       ├── consts.go          # 房间状态、结束原因、错误码
│       └── board.go           # 棋盘工具（校验、胜负判定）
├── deploy/
│   ├── docker-compose.yml     # 本地 Redis（及可选 MySQL）
│   └── systemd/               # 生产 systemd 单元示例
├── web/
│   ├── index.html             # 最小可运行示例客户端（见 §7.7）
│   └── lib/
│       ├── protocol.js        # starx 官方协议编解码（原样引入，勿改）
│       └── starx-wsclient.js  # starx 官方 WebSocket 客户端（原样引入，勿改）
└── docs/
    ├── gomoku.proto           # 协议定义（本仓库唯一协议源）
    └── schema.sql             # MySQL 建表脚本
```

---

## 5. 核心机制设计

### 5.1 对局状态机

```
  IDLE ──C2SJoinMatch──► MATCHING ──撮合成功──► PLAYING ──终局──► SETTLING ──► IDLE
                          │  ▲                     │ ▲
                          │  └─C2SCancelMatch       │ └─(结算完成, 房间销毁)
                          └──超时未撮合(可配置, 默认30s)──► IDLE
```

房间（Room）内部状态：`ROOM_IDLE → ROOM_PLAYING → ROOM_OVER`。

- 每个房间在内存中持有：`board [15][15]int8`、`black/white uid`、`turnSeat`、`round`、双方 `stepRemain/totalRemain`、`aiTakeover[2]bool`、`moves []Move`、定时器句柄。
- 所有修改都经过房间管理器加锁（`sync.Mutex` 每房间一把，天然避免跨房间争用）。

### 5.2 限时下棋

**规则**（参数化，见 §10 `game` 段）：

| 参数 | 默认值 | 说明 |
| --- | --- | --- |
| `step_time_limit` | 180s | 每步限时（3 分钟），回合切换时重置；超时判负 |
| `total_time_limit` | 600s | 每方总用时上限，0 表示不限制（当前版本暂未启用，预留） |
| `timer_push_interval` | 1s | 倒计时推送周期；剩余 ≤5s 时改为 200ms |

**实现要点（服务器权威计时）**：

1. 回合切换（开局或某方落子成功）时启动该方计时器 `time.AfterFunc`。
2. 计时器触发且房间仍为 `PLAYING` → 判定超时方负，广播 `room.onGameOver(winner=对方, reason=TIMEOUT)`，进入结算。
3. 客户端展示依赖 `room.onTimer` 推送，服务器不做客户端时间信任。
4. 掉线宽限期间计时**不暂停**：掉线方轮到时宽限期结束后 AI 立即接盘并在其剩余步时内落子，防止拖局。

> 伪代码：
> ```go
> func (r *Room) startTurnTimer(seat int8) {
>     if r.stepTimer != nil { r.stepTimer.Stop() }
>     r.stepTimer = time.AfterFunc(r.stepTimeLimit, func() {
>         r.mu.Lock(); defer r.mu.Unlock()
>         if r.state != ROOM_PLAYING { return }
>         r.finish(r.opponent(seat), EndReasonTimeout) // 超时判负
>     })
> }
> ```

### 5.3 掉线检测与 AI 接管

**判定流程**：

```
客户端心跳(5s)
   │ 服务器 15s 未收到心跳 / 收到 C2SLeaveGame
   ▼
判定掉线 ──广播 room.onPlayerLeave(seat, reason=OFFLINE|LEAVE)
   │
   ▼
宽限期 AI_TAKEOVER_GRACE=10s（计时继续走）
   ├── 宽限期内重连 ──► 恢复控制（room.onPlayerBack 广播，状态还原）
   └── 宽限期结束 ──► 标记 aiTakeover[seat]=true
                       广播 room.onAITakeOver(seat)
                       ├── 若轮到此方：AI 立即计算落子（AI 用时计入其步时）
                       └── 否则：轮到该方时由 AI 落子
```

**关键规则**：

| 规则 | 默认行为 | 配置项 |
| --- | --- | --- |
| 心跳超时阈值 | 3 次未收到（5s 一次） | `heartbeat_timeout=15s` |
| 宽限期 | 10s | `ai_takeover_grace` |
| 接管后重连 | 转为**观战**（防止"断线骗取 AI 代打"），可配置允许夺回控制 | `ai_takeover_return_control=false` |
| 胜负归属 | AI 只是掉线玩家的代理，终局胜负正常结算给该玩家 | — |
| 逃跑惩罚 | 默认不额外扣分，`game_record.ai_takeover_seat` 记录接管事实；可配置开启 | `ai_takeover_penalty`（预留） |

> 设计取舍：若运营希望"离开即判负"，只需把 `ai_takeover_grace` 设为 0 并关闭 AI 接管，走 `reason=LEAVE` 直接结算，两种策略文档均覆盖、代码按配置分支即可。

**服务器侧**：
- 心跳检测在 gate 的 session 层实现（Pitaya 支持对 session 做超时监听），掉线事件通过 RPC 通知房间归属的 room server。
- 房间在 Redis 镜像 `room:{roomId}`（含 `ai_black/ai_white` 标志），重连请求按 `server_id` 定向到原 room server。

### 5.4 AI 落子算法

**输入**：当前棋盘、AI 方（黑/白）、难度档。
**输出**：合法落子坐标 `(x, y)`。**性能约束**：单次落子 ≤ 2s。

**v1 方案：棋型评分启发式 + 可选 α-β 搜索**

1. **棋型评分表**（对每个候选空位，沿 4 个方向 × 黑白两色统计连子）：

| 棋型 | 分值 |
| --- | --- |
| 五连 FIVE | 10,000,000 |
| 活四 LIVE_FOUR | 1,000,000 |
| 冲四 RUSH_FOUR | 100,000 |
| 活三 LIVE_THREE | 10,000 |
| 眠三 SLEEP_THREE | 1,000 |
| 活二 LIVE_TWO | 100 |
| 眠二 SLEEP_TWO | 10 |

2. **评分公式**：`score(x,y) = attack(x,y) + defense(x,y) × 0.9`，其中 `attack` 假设己方落子于此、`defense` 假设对方落子于此；取最高分空位落子。仅考察以 (x,y) 为中心 5×5 邻域内的候选点，剪掉远端空位。
3. **难度档位**：
   - 简单（1）：启发式 + 少量随机扰动；
   - 普通（2）：启发式 + 对评分前 N 个候选做 2 层极小极大；
   - 困难（3）：α-β 剪枝、深度 4、按启发式排序走子（配合 `search.go`）。
4. **确定性保障**：AI 结果与房间状态在同一锁内计算并落子，保证与玩家落子互斥。

> 后续可扩展：开局库（前 6 手）、蒙特卡洛树搜索（MCTS）、禁手规则支持。AI 引擎与对局逻辑解耦（`internal/ai` 只依赖棋盘快照），便于单独单测与替换。

### 5.5 结算与排行

- 终局（五连 / 超时 / 离开判负 / AI 定局）→ `settle.go`：
  1. 写 `game_record`（含 `moves` JSON、`end_reason`、`ai_takeover_seat`）；
  2. 更新 `player_stats`（胜/负/平、ELO，K=32，初始 1200，同分匹配池内异步重算）；
  3. 更新 Redis `rank:elo` zset 与 `stat:user:{uid}` 缓存；
  4. 广播 `room.onGameOver`（携带完整走子记录供回放）；
  5. 清理房间（内存 + Redis key + 匹配队列残留）。
- 排行榜查询 `rank.list`：读 Redis zset 逆序分页，兜底回源 MySQL。

---

## 6. 消息协议（Protobuf）

### 6.1 帧格式与序列化

- 传输帧沿用 Pitaya 默认协议：`1B type + 3B bodyLen + route + body`；body 为序列化器编码结果。
- 序列化器：正式环境 Pitaya 配置 `serialize.ProtobufSerializer`；联调期可切 `pitaya.serializertype: 1`（JSON，Pitaya 默认值）以配合官方 starx JS 客户端开箱即用（见 §7.5）。前后端共用 `docs/gomoku.proto`，经 `protoc --go_out` 生成。
- 消息分三类：**request**（客户端请求，服务端 response 回包）、**push**（服务端主动推送）、**notify**（客户端单向通知）。

### 6.2 路由表

| 方向 | 路由 | 消息 | 说明 |
| --- | --- | --- | --- |
| req/resp | `auth.login` | `C2SLogin` → `S2CLogin` | 登录，返回 uid/token/基础信息 |
| req/resp | `match.join` | `C2SJoinMatch` → `S2CJoinMatch` | 进入匹配队列 |
| req/resp | `match.cancel` | `C2SCancelMatch` | 取消匹配 |
| push | `room.onMatchSuccess` | `S2CMatchSuccess` | 撮合成功（异步下发） |
| req/resp | `room.place` | `C2SPlacePiece` → `S2CPlacePiece` | 落子（含校验失败回包） |
| req/resp | `room.leave` | `C2SLeaveGame` → `S2CLeaveGame` | 主动离开 |
| req/resp | `room.reconnect` | `C2SReconnect` → `S2CReconnect` | 掉线重连恢复 |
| push | `room.onTurn` | `S2CTurnChange` | 回合切换 |
| push | `room.onTimer` | `S2CTimer` | 倒计时推送 |
| push | `room.onPlace` | `S2CPlacePiece` | 落子广播（所有观战/双方） |
| push | `room.onPlayerLeave` | `S2CPlayerLeave` | 对方掉线/离开 |
| push | `room.onPlayerBack` | `S2CPlayerBack` | 对方宽限期内重连恢复 |
| push | `room.onAITakeOver` | `S2CAITakeOver` | AI 接管通知 |
| push | `room.onGameOver` | `S2CGameOver` | 终局广播 |
| req/resp | `rank.list` | `C2SRankList` → `S2CRankList` | 排行榜分页 |

> 完整 message 定义见 **`docs/gomoku.proto`**（本仓库协议唯一事实源）。

---

## 7. 客户端接入（Web / starx JS）

### 7.1 客户端库选型与落地

- 采用 **starx 官方 JavaScript 客户端**：[nano-ecosystem/starx-client-websocket](https://github.com/nano-ecosystem/starx-client-websocket)（MIT License）。
  starx 是 Pitaya 的前身，两者网络协议完全兼容（Pitaya 即从 starx 演进而来）。
- ⚠️ 注意：`topfreegames/pitaya-client-websocket` 链接已失效（404），官方维护的 JS 客户端即上述 starx 仓库。
- 客户端能力：WebSocket 连接、握手、心跳维护、断线自动重连、`request`/`notify` 发送、`on` 监听服务端 push、路由压缩字典解析。
- 已随本仓库落地 `web/lib/`（来源与**一处必要补丁**说明见 `web/lib/README.md`；补丁：Pitaya 服务端下发的空 body 心跳包会导致上游 `Package.decode` 崩溃，已修复）：
  - `protocol.js`：pomelo/starx 二进制协议编解码（Package 分帧 + Message 消息头），暴露全局 `window.Protocol`；
  - `starx-wsclient.js`：连接封装，暴露全局 `window.starx`（`init`/`request`/`notify`/`on`/`disconnect`）。
- 最小可运行示例：`web/index.html`（见 §7.7）。

### 7.2 初始化与生命周期

```html
<script src="./lib/protocol.js"></script>
<script src="./lib/starx-wsclient.js"></script>
<script>
starx.init({
  host: "127.0.0.1",        // 服务端 gate 地址
  port: 3250,               // Pitaya 监听端口（ws）
  reconnect: true,          // 断线自动重连（指数退避，默认最多 10 次）
  maxReconnectAttempts: 10,
  user: { platform: "web", version: "0.0.1" } // 随握手带给服务端，存入 session
}, function () {
  // 握手成功回调（连接建立后触发）
  console.log("connected");
});
</script>
```

- **握手**：客户端首包为 handshake，服务端回包携带心跳间隔（`sys.heartbeat`，秒）与路由压缩字典（`sys.dict`）；客户端自动回 handshake ack 后连接才可用。握手回调即上述 `init` 的第二个参数。
- **心跳**：由库根据服务端下发的间隔自动发送，无需业务代码介入。
- **生命周期事件**（`starx.on(event, cb)`）：`disconnect`、`reconnect`（库内自动重连成功）、`heartbeat timeout`、`io-error`。
- **主动断开**：`starx.disconnect()`。
- 注：官方示例中的 `log: true` 为历史遗留参数（库内固定输出 console 日志），不传即可。

### 7.3 路由映射表（客户端视角）

> 路由格式：`{serverType}.{service}.{method}`（三段式，集群）或 `{service}.{method}`（两段式，单机）。
> 单机开发（`app.server_type: room`）下客户端用**两段式小写**路由，Pitaya 自动把 serverType 补为本地进程类型后本地处理；
> 集群模式按 gate/room/rank 拆分部署，客户端用**三段式**路由。
> 注意：Pitaya v2 对 `service.method` 段**精确匹配、不做大小写折叠**；本服务端注册时使用
> `component.WithName("auth"...)` + `component.WithNameFunc(strings.ToLower)`，组件名与方法名统一为小写。

| 操作 | 单机路由（两段式） | 集群路由（三段式） | 方向 |
| --- | --- | --- | --- |
| 登录 | `auth.login` | `gate.auth.login` | req/resp |
| 进入匹配 | `match.join` | `room.match.join` | req/resp |
| 取消匹配 | `match.cancel` | `room.match.cancel` | req/resp |
| 落子 | `room.place` | `room.room.place` | req/resp |
| 主动离开 | `room.leave` | `room.room.leave` | req/resp |
| 断线重连 | `room.reconnect` | `room.room.reconnect` | req/resp |
| 排行榜 | `rank.list` | `rank.rank.list` | req/resp |
| 撮合成功 | `room.onMatchSuccess` | `room.onMatchSuccess` | push |
| 回合切换 | `room.onTurn` | `room.onTurn` | push |
| 倒计时推送 | `room.onTimer` | `room.onTimer` | push |
| 落子广播 | `room.onPlace` | `room.onPlace` | push |
| 对方掉线/离开 | `room.onPlayerLeave` | `room.onPlayerLeave` | push |
| 对方重连恢复 | `room.onPlayerBack` | `room.onPlayerBack` | push |
| AI 接管 | `room.onAITakeOver` | `room.onAITakeOver` | push |
| 终局广播 | `room.onGameOver` | `room.onGameOver` | push |

- push 路由是服务端显式字符串（`pitaya.SendPushToUser(route, ...)`），单机/集群一致（服务端代码写死），客户端按该完整字符串监听。
- 服务端启动日志会打印实际注册的 handler 路由（`registered handler auth.login, isRawArg: false` 等），客户端路由以之为准；也可用 `app.Documentation(true)` 导出完整清单核对。

### 7.4 消息收发示例

```js
// request：登录（回包进回调）
starx.request("gate.Auth.Login", { username: "u1", password: "p1" }, function (data) {
  // data = S2CLogin：{ code, msg, uid, token, nickname, elo, wins, losses, draws }
});

// request：落子
starx.request("room.Room.Place", { x: 7, y: 7 }, function (data) {
  // data = S2CPlacePiece：{ code, msg, seat, x, y, round }
});

// notify：单向通知（无回包）。本项目协议暂未定义 notify 路由，此行仅为演示 API 形态
starx.notify("room.Room.Ping", {});

// push：监听服务端推送（事件名 = 完整路由，见 §7.3）
starx.on("room.Room.onTurn", function (data) {
  // data = S2CTurnChange：{ seat, step_remain_ms, total_remain_ms }
});
starx.on("room.Room.onGameOver", function (data) {
  // data = S2CGameOver：{ winner, reason, end_at, moves, ai_takeover_seat }
});
```

- 回调/推送对象字段名与 `docs/gomoku.proto` 中 message 字段一一对应（snake_case，如 `step_remain_ms`）；若走 §7.5 路径 B（protobufjs），解码时用 `keepCase: true` 即可保持同名，业务代码不变。
- request 失败（服务端返回错误码）时回调 `data.code != 0`，错误码表见 proto 文件末尾注释。

### 7.5 序列化：JSON 与 Protobuf

官方 starx JS 客户端**默认使用 JSON 序列化**（`JSON.stringify` + UTF-8，无额外依赖），因此：

| 路径 | 客户端 | 服务端（Pitaya v2） | 适用 |
| --- | --- | --- | --- |
| A. JSON（推荐联调） | 开箱即用，无需任何配置 | `pitaya.serializertype: 1`（JSON 即 Pitaya 默认值） | v1 联调/快速打通，字段名与 proto 对齐 |
| B. Protobuf（正式） | `starx.init({ encode, decode })` 注入 protobufjs 编解码（见下） | `pitaya.serializertype: 2`（protobuf） | 生产环境、流量/带宽敏感 |

> 两种路径下业务代码不变：字段名均以 `docs/gomoku.proto` 为准，切换只影响网络层编解码。
>
> 当前实现采用**路径 A**（JSON）。注意：Pitaya v2.11.24 默认对消息体做 zlib 压缩
> （`pitaya.handler.messages.compression` 默认 `true`），而官方 starx 客户端不处理压缩消息，
> 因此 `config.yaml` 中**必须**显式置为 `false`（见 §10），否则客户端无法解析服务端响应。

路径 B 客户端注入骨架（以 `protobufjs` + `pbjs` 生成的 `gomoku.js` 为例）：

```js
// 生成：pbjs -t static-module -w commonjs -o gomoku.js docs/gomoku.proto
var gomoku = require("./gomoku.js").gomoku;
// 路由 → protobufjs message 类型映射（C2S 用于 encode，S2C/push 用于 decode）
var C2S = { "gate.Auth.Login": gomoku.C2SLogin, "room.Room.Place": gomoku.C2SPlacePiece /* ... */ };
var S2C = { "room.Room.onTurn": gomoku.S2CTurnChange, "room.Room.onGameOver": gomoku.S2CGameOver /* ... */ };

starx.init({
  host: host, port: port,
  encode: function (reqId, route, msg) {
    // 浏览器下 Protocol.Message.encode 的 body 参数为 Uint8Array，protobufjs finish() 可直接给
    var T = C2S[route];
    var body = T ? T.encode(T.create(msg)).finish()
                 : window.Protocol.strencode(JSON.stringify(msg)); // 未注册路由回退 JSON
    return window.Protocol.Message.encode(reqId, reqId ? window.Protocol.Message.TYPE_REQUEST
                                                        : window.Protocol.Message.TYPE_NOTIFY,
                                          0, route, body);
  },
  decode: function (data) {
    var m = window.Protocol.Message.decode(data);
    // 请求回包不携带 route，需按 msg.id 从客户端自己维护的 reqId→route 映射还原（默认实现
    // 内部 routeMap 不可见，需在 request 封装处记录）；push 消息自带 route，可直接查表。
    var T = m.route && S2C[m.route];
    m.body = T ? T.decode(m.body) : JSON.parse(window.Protocol.strdecode(m.body));
    return m;
  }
}, function () { /* connected */ });
```

> 说明：`encode`/`decode` 是 starx 客户端的官方扩展点（`init` 参数覆盖默认 JSON 实现，默认实现见 `starx-wsclient.js` 的 `defaultEncode`/`defaultDecode`）。v1 若选路径 A，此节可完全忽略。

### 7.6 心跳与掉线重连

- **心跳**：库自动发送（间隔由服务端握手 `sys.heartbeat` 下发）。本项目 `game.heartbeat_timeout=15s` 对应"3 次未收到心跳判定掉线"（见 §5.3）。
- **网络层重连**：`init({ reconnect: true })` 开启库内自动重连（指数退避：5s→10s→…，上限 `maxReconnectAttempts` 次），断线期间触发 `disconnect` 事件、重连成功触发 `reconnect` 事件。
- **对局层重连**：网络重连只是恢复连接，**不会自动恢复对局状态**。对局中掉线需调用 `room.Room.Reconnect`（`{ roomId, token }`）拉取全量状态（`S2CReconnect`：state/seat/moves[]/turn_seat/remain_ms/ai_takeover/spectator），据此重建棋盘与计时 UI（见 §9.5）。
- 掉线后服务端行为（宽限期、AI 接管、观战规则）见 §5.3，与客户端库无关。

### 7.7 本地联调步骤

1. 按 §11.1 启动服务端（Redis + MySQL + `go run ./cmd/server`）。
2. 浏览器直接打开 `web/index.html`（无需构建步骤；依赖浏览器 `WebSocket`/`ArrayBuffer`）。
3. 输入 `127.0.0.1:3250` → 连接 → 登录 → 进入匹配；开两个标签页用两个账号即可对弈（默认种子账号 `player1`/`player2`，密码均 `123456`，见 `docs/schema.sql`）。
4. 验证项：双方落子同步、倒计时推送、`F12` 断网模拟掉线 → 库自动重连 + `room.Room.Reconnect` 恢复棋盘。

---

## 8. 数据设计

### 8.1 MySQL（远程 `47.97.42.179:3306`，库 `gomoku`）

| 表 | 用途 | 关键字段 |
| --- | --- | --- |
| `user` | 账号与角色基础数据 | id、username(唯一)、password_hash、nickname、avatar、created_at、updated_at |
| `player_stats` | 战绩与积分 | user_id(唯一)、wins、losses、draws、elo、runaways、updated_at |
| `game_record` | 对局记录（含回放） | id、room_id、black_uid、white_uid、winner、end_reason、ai_takeover_seat、moves(JSON)、started_at、ended_at |

- 建表脚本：**`docs/schema.sql`**；字符集 `utf8mb4`；`game_record` 按 `started_at` 建索引，便于历史分页。
- 写入策略：对局热路径不落库；结算在房间进程内异步写库（队列 + 批量，防止结算阻塞对局回收）。

### 8.2 Redis（本地 `127.0.0.1:6379`）key 设计

| Key | 类型 | 说明 | 生命周期 |
| --- | --- | --- | --- |
| `online:{uid}` | string | 在线状态（`1`） | TTL 60s，心跳续期 |
| `session:{token}` | string | 登录态（uid、昵称） | TTL 7d |
| `room:{roomId}` | hash | 房间摘要：state、black/white uid、turn、round、step_remain、total_remain、ai_black/ai_white、server_id | 对局结束清理 |
| `room:{roomId}:moves` | list | 走子序列（重连恢复用） | 对局结束清理 |
| `match:queue` | list | 匹配等待队列（存 uid，`LPOP` 原子撮合） | 随匹配清空 |
| `rank:elo` | zset | member=uid，score=elo | 常驻 |
| `stat:user:{uid}` | hash | 战绩缓存（wins/losses/draws/elo） | TTL 1h |

- 并发注意：匹配撮合用 `LPOP` 保证同一对手只被一个 room server 取到；房间创建用 `SET room:{id} NX` 防重。

---

## 9. 关键流程时序

> 以下时序中的路由为逻辑路由（见 §6.2）；客户端实际调用的完整路由（含大小写）见 §7.3。

### 9.1 登录与连接

```
Client ──WS connect──► gate
Client ──auth.login(C2SLogin)──► gate.auth 组件
gate ──校验密码(MySQL user)──► 生成 token，绑定 uid↔session
gate ──resp S2CLogin(uid, token, nickname, elo)──► Client
gate ──Redis SET session:{token} / online:{uid}──►
```

### 9.2 匹配 → 开局

```
Client ──match.join──► room(任一) ──LPUSH match:queue(uid)──► Redis
撮合成功(另一玩家)：
roomA ──LPOP 取到对手──► 创建房间：内存 + Redis room:{id}(server_id=roomA)
roomA ──push room.onMatchSuccess──► 双方(seat 0/1 随机或按 ELO 先后手)
roomA ──push room.onTurn(seat=0, remain)──► 黑方开始计时
```

### 9.3 落子（服务器权威）

```
Client ──room.place(C2SPlacePiece{x,y})──► room
校验：房间状态 PLAYING｜轮次==seat｜(x,y)∈[0,15)｜board[x][y]==空｜非观战
通过 → 落子 → 胜负判定(common/board.go)
  ├─ 五连 ──► finish(winner, reason=WIN) → 结算(§5.5) → push room.onGameOver
  └─ 未终局 ──► 切换 turn → 重置步时 → push room.onPlace + room.onTurn → 启动计时
不通过 → resp 错误码（见 proto `code` 字段）
```

### 9.4 掉线 → AI 接管 → 重连

```
gate 心跳超时 → RPC 通知 room(server_id) → 房间标记 seat 掉线
push room.onPlayerLeave(seat, OFFLINE) → 启动 10s 宽限计时
  ├─ 宽限内：Client 重连 → room.reconnect(C2SReconnect{room_id, token})
  │   校验通过 → push room.onPlayerBack → 恢复其落子权（如轮到自己且 AI 未接管）
  └─ 逾期：aiTakeover[seat]=true → push room.onAITakeOver(seat)
      AI 按 §5.4 在轮到该方时落子（与玩家落子同锁互斥）
终局 → game_record.ai_takeover_seat 记录接管座位
```

### 9.5 重连恢复（全量状态还原）

`S2CReconnect` 返回：`state`、`seat`、`moves[]`（来自 Redis list）、`turn_seat`、`remain_ms`、`ai_takeover`、`spectator`、双方 uid → 客户端据此重建棋盘与计时 UI。

---

## 10. 配置示例（`config/config.yaml`）

```yaml
pitaya:
  serializertype: 1          # 1=JSON（默认，starx 客户端开箱即用） 2=protobuf；见 §7.5
  heartbeat:
    interval: 5s             # 服务端下发的心跳间隔，客户端据此自动心跳
  handler:
    messages:
      compression: false     # 必须为 false：官方 starx 客户端不处理 zlib 压缩消息（Pitaya 默认 true）

app:
  name: gomoku
  server_type: room        # gate | room | rank（单机开发可只跑 room 并内置全部组件）
  frontend: false          # gate 为 true
  port: 3250
  grpc_port: 3430
  cluster: false           # 生产置 true 并配置 nats

mysql:
  dsn: "root:8A35Nn3BkrCpny65@tcp(47.97.42.179:3306)/gomoku?charset=utf8mb4&parseTime=true&loc=Local"
  max_open: 50
  max_idle: 10

redis:
  addr: "127.0.0.1:6379"
  password: ""
  db: 0

game:
  board_size: 15
  step_time_limit: 180      # 秒（每步限时 3 分钟，回合切换时重置；超时判负）
  total_time_limit: 600     # 秒，0=不限（当前版本暂未启用，预留）
  timer_push_interval: 1   # 秒；剩余≤5s 时自动加密为 200ms
  heartbeat_timeout: 15    # 秒
  ai_takeover_grace: 10    # 秒
  ai_takeover_return_control: false
  ai_level: 2              # 1 简单 / 2 普通 / 3 困难
  forbid_rules: false      # 禁手规则（v1 关闭，预留）
```

> 敏感配置（MySQL 密码等）支持从环境变量覆盖：`GOMOKU_MYSQL_DSN`、`GOMOKU_REDIS_ADDR` 等。

---

## 11. 部署

### 11.1 本地开发

1. 启动 Redis：`docker run -d -p 6379:6379 redis:7`（或 `docker compose -f deploy/docker-compose.yml up -d`，可同时起 MySQL 测试库）。
2. 初始化库表：对 MySQL 执行 `docs/schema.sql`（含种子账号 `player1`/`player2`/`ai_test`，密码均 `123456`）。
3. `go build ./...`（依赖 Go 1.25+）→ `go run ./cmd/server -c config/config.yaml`。
4. 客户端连 `ws://127.0.0.1:3250`（TCP 同端口按 Pitaya 配置）；Web 端直接浏览器打开 `web/index.html`（联调步骤见 §7.7）。
5. 快速自检：服务端启动日志出现 `registered handler auth.login` 等 7 条路由注册、`Acceptor ... is now accepting connections`；用浏览器示例页登录 `player1/123456` 应返回 `code=0`。

### 11.2 生产

- `gate` 无状态水平扩展，置于 Nginx/LB 后（wss 443 终结）。
- `room` 按房间分片多开：`server_id` 唯一，新房间优先调度到负载最低实例；重连按 Redis `room:{id}.server_id` 定向。
- 部署包：`go build -trimpath -ldflags "-s -w" ./cmd/server`，systemd 托管（示例见 `deploy/systemd/`）；日志 stdout + JSON，接采集。
- 运维指标：房间数、在线数、匹配等待时长、AI 接管次数、对局耗时 P95；落库延迟与失败计数告警。

---

## 12. 开发计划与里程碑

| 里程碑 | 内容 | 验收要点 |
| --- | --- | --- |
| M1 工程骨架 | go.mod(go 1.25)、Pitaya 单机启动、配置加载、MySQL/Redis 连通、protoc 生成链路 | ✅ 已实现：`go build` 通过；WS 启动；MySQL/Redis 失败降级告警不阻塞 |
| M2 登录与连接 | `auth.login`、token、session 绑定、心跳；starx JS 客户端接入（`web/index.html`） | ✅ 已实现：`auth.login` 端到端验证通过（含**登录即注册**：无账号自动建档）；心跳/握手正常 |
| M3 匹配与对局 | 匹配队列、房间创建、落子校验、胜负判定、广播 | ✅ 已实现：撮合循环（Redis 原子配对）→ 房间创建 → `room.onMatchSuccess`/`room.onTurn`/`room.onPlace`/`room.onGameOver` 推送；落子校验（回合 3001/坐标 3002）与五连判胜端到端验证通过 |
| M4 限时与掉线 | 步时/总时计时、超时判负、心跳检测、宽限期、AI 接管、重连恢复 | ⏳ 部分实现：每步计时（默认 180s）+ `room.onTimer` 倒计时推送（≤5s 加密 200ms）+ 超时判负 + `room.leave` 离开判负 + `room.reconnect` 掉线重连恢复（token 校验→全量状态还原→新连接绑定→`room.onPlayerBack` 通知）均已实现并验证；掉线检测/AI 接管待实现 |
| M5 排行与数据 | `player_stats`/`game_record` 落库、ELO、`rank.list`、Redis 缓存 | ⏳ 待实现（`rank.list` 骨架已注册） |
| M6 联调与压测 | 集群模式（NATS）、多 room、压测（落子 RPS、万级在线）、AI 强度调参 | 压测达标；无已知竞态 |

---

## 13. 验收标准（v1.0）

- [ ] 两名玩家可经 WebSocket 完成匹配并完整对弈，黑先白后，五连判定准确（含边界：贴边五连、双活四）。
- [ ] 每步限时 30s：倒计时 UI 与服务器一致；超时方自动判负并广播终局。
- [ ] 任一玩家掉线 ≥10s 后，AI 接管落子且对局不中断；AI 落子均合法（空位、范围内）。
- [ ] 宽限期内重连可恢复控制权并还原棋盘/计时；接管后重连为观战模式。
- [ ] 终局正确写入 `game_record`（含 `ai_takeover_seat` 与 moves 回放），`player_stats` 与排行榜更新一致。
- [ ] 服务端对落子做权威校验，非法落子（越界、重复占位、非己方回合、观战落子）均被拒绝。
- [ ] 协议字段与 `docs/gomoku.proto` 完全一致：JSON 路径字段名对齐（snake_case）；protobuf 路径前后端同一份定义生成代码。
- [ ] `web/index.html` 使用官方 starx JS 客户端（`web/lib/`）可完成 连接→登录→匹配→对弈→结算 全流程；客户端路由与 §7.3 一致。
- [ ] 客户端断线自动重连（网络层），对局中掉线经 `room.Room.Reconnect` 恢复棋盘与计时状态。

---

## 14. 风险与后续扩展

| 项 | 说明 |
| --- | --- |
| 防作弊 | v1 仅做服务器权威校验；后续可加对局 hash 校验、战绩风控（异常胜率检测） |
| AI 强度 | 启发式在开局/中盘可能偏弱，可迭代棋型权重或引入 MCTS |
| 断线重连跨实例 | 依赖 Redis 房间镜像与 `server_id` 定向；若 room 实例宕机需房间迁移（M7 预留） |
| 数据一致性 | 结算写库采用"先 Redis 后异步落库"，需配套补偿任务（未落库对局回扫） |
| 客户端维护 | 官方 starx JS 客户端为社区维护（MIT）、更新不活跃；协议层（pomelo/starx）稳定，风险低，必要时可 fork 自维护或迁移至 protobufjs + 自研薄封装 |
| 扩展玩法 | 好友房、观战、悔棋、聊天、禁手规则、赛季制——协议与状态机均已预留扩展位 |

---

*附：协议源文件 `docs/gomoku.proto`、建表脚本 `docs/schema.sql`，均以本仓库内文件为准。*
