# 学习笔记（五子棋项目）

> 按学习纪律第 4 条沉淀答疑。每阶段遇到的问题、踩过的坑、老师讲解的原理都记在这里。

---

## 阶段 0：环境

### 坑 1：Pitaya 与 viper 版本冲突（编译失败）

- **现象**：`go build ./...` 报 `cannot use c (variable of type *mitchellh/mapstructure.DecoderConfig) as *go-viper/mapstructure/v2.DecoderConfig`（位于 pitaya 的 `config/viper_config.go`）
- **原因**：`spf13/viper` 从 v1.20 起底层换成 `go-viper/mapstructure/v2`；而 Pitaya v2.11.24 内部仍引用旧包 `mitchellh/mapstructure`，两者类型对不上
- **解决**：viper 降到 v1.15.0（与 FIVE 参考实现一致），`go mod tidy` 后 `go-viper/mapstructure/v2` 自动移除
- **教训**：框架对依赖版本有隐性要求时，先看该框架自己的 `go.mod` 声明了什么版本，不要直接上最新版

---

## 阶段 2：棋盘核心

### 原理 1：为什么判胜只需查落子点所在的四条线

- 反证法：若存在一个**不包含最新落子**的五连，那它在上一手就存在，上一手落子时 CheckWin 就该返回 true，游戏已结束
- 结论：游戏继续 ⇒ 不存在旧五连 ⇒ 新五连必然穿过落子点 ⇒ 从落子点沿横/竖/两斜 4 个方向双向数同色子，≥5 即胜

### 坑 2：`inBoard` 边界用 `<=` 导致数组越界 panic

- **现象**：测试"贴右下角"用例 panic：`index out of range [15] with length 15`
- **原因**：`inBoard` 写成 `x <= BoardSize`，当 x=15（等于 BoardSize）时仍返回 true，`b[15][14]` 越界
- **正确**：`x >= 0 && x < BoardSize && y >= 0 && y < BoardSize`
- **教训**：棋盘下标合法范围 0..14，边界判断必须用 `<` 而非 `<=`；贴边/角落用例专门抓这类问题

### 写法 1：表驱动测试（Go 惯例）

- `tests := []struct{ name; pieces; x, y; color; want }`：用例数据与执行逻辑分离
- `t.Run(tt.name, ...)` 子测试：失败时直接显示是哪个场景
- `makeBoard(pieces [][3]int8)` helper：`{x, y, color}` 三元组摆子，避免重复赋值
- 测试要覆盖：四条线五连、活四/冲四 false、长连 true（`>=` 而非 `==`）、贴边、角落、颜色参数不符、空棋盘

### 差异 1：我的实现 vs FIVE（对照读结论）

| 维度 | 我 | FIVE | 评析 |
| --- | --- | --- | --- |
| 坐标/方向类型 | `int8` | `int` | 省内存；但阶段 5 前端 JSON 坐标是 int，需显式转换 |
| 双向计数 | 抽 `countDir` 辅助函数 | 内联两个 for 循环 | 我的可读性更好；Go 编译器自动内联小函数，无性能差 |
| 方向数组位置 | package 级 `var dir` | CheckWin 内部局部变量 | FIVE 封装更好，方向数组不应暴露为包级变量 |

---

## 阶段 3：auth 登录

### 原理 1：消息命名 C2S / S2C

- `C2SLogin` = Client-to-Server（客户端请求体，只有 username/password）；`S2CLogin` = Server-to-Client（响应体，10 个字段）
- 请求/响应字段不对称 → 必须拆两个结构，一个结构 = 一个方向的一次消息
- push（服务器主动推送）只有单向，不叫 C2S/S2C，直接叫业务名（room.onMatchSuccess 等）
- 消息结构放 `model` 包统一管理，组件方法签名 `func (c *X) Method(ctx, in *model.C2SXxx) (*model.S2CXxx, error)`

### 原理 2：bcrypt / token / session 三者关系

- bcrypt：慢哈希 + 随机盐，防彩虹表/撞库；明文和 MD5 都不行
- token：服务器签发的随机 hex 串（"门票"），存 Redis `session:{token}`，TTL 7 天
- Pitaya session：uid ↔ 连接 的绑定（"座位"），存内存，断连即失效
- 登录流程：校验 → 查库 → 注册/校验密码 → newToken → SetSession/SetOnline → s.Bind(uid) → 回包

### 原理 3：登录即注册的并发安全（唯一键冲突回查）

- 两个请求同时注册同一新用户名 → 后者撞 `uk_username` 唯一键
- 正确做法：`CreateUser` 失败后**回查**，按既有账号继续走密码校验（不报错）
- 结构必须是 `if err != nil { 回查 } else { 新建 }` 互斥分支；"无条件执行"的赋值会覆盖回查结果（uid=0 bug）

### 坑 3：注册分支 u 未回填 → nil pointer panic

- `u == nil` 分支里 `CreateUser` 成功后没给 `u` 赋值，后续 `u.ID`/`u.Nickname` 直接 panic
- 修复：`u = &model.User{ID: newUserId, ...}` 回填；`CreateUser` 的 uid 返回要用变量接住，不能 `_` 丢弃

### 原理 4：JSON body vs 二进制帧（联调期困惑）

- **序列化（JSON/protobuf）只管 body 怎么编码；starx 协议壳（二进制）管怎么传输**，两者独立
- 前端 DevTools 看到二进制帧是正常的：Package 头 4B + Message 头（flag/id/route）+ body(JSON)
- 证据：握手响应 `sys={"serializer":"json"}`；starx-wsclient.js `socket.binaryType='arraybuffer'`
- 看 JSON 的办法：① 服务端 logrus 打印 ② 页面日志区 ③ DevTools 里 Protocol.Package/Message.decode 逐层剥壳

### 坑 4：starx 协议测试脚本要点

- `protocol.js` 在 Node 里可直接 `require`（模块尾部判断 `typeof window === "undefined"`）
- `Package.decode` 单包返回**对象**、多包返回**数组**（`rs.length === 1 ? rs[0] : rs`），要 `Array.isArray` 判断
- 握手 → 收 code=200 → 发 HANDSHAKE_ACK → 才能发请求；心跳间隔从握手响应 sys.heartbeat 拿

### 阶段 3 实机验收记录（全部通过）

| 用例 | 输入 | 结果 |
| --- | --- | --- |
| 新用户注册 | test_*/123456 | code=0 uid=7 token=64字符 elo=1200 |
| 同一账号再登录 | 同上 | code=0 uid=7（一致，不重复建号） |
| 错误密码 | test_*/wrongpass | code=1001 用户名或密码错误 |
| 空参数 | ""/"" | code=1001 用户名或密码不能为空 |

- 握手响应 `sys={"dict":{},"heartbeat":5,"serializer":"json"}` 实锤 body 是 JSON
- 测试工具：Node 脚本 require 前端 `web/lib/protocol.js`，模拟 starx 客户端全流程

---

## 阶段 4：match 匹配组件

### 原理 1：为什么用 Redis 列表做匹配队列

- Redis List 原子操作（LPUSH/RPOP/LREM）天然无锁；内存 []int64 要自己加锁且跨进程不可见、重启丢人
- `LPUSH` 入队 + `RPop` 撮合 = FIFO 先到先配；方向约定全项目统一
- 原子弹出保证同一对手不被两个撮合进程抢到（集群模式）

### 原理 2：撮合循环三个边界（易写漏）

1. 队列空 → 静默返回等下一个 tick（RPop 报 redis.Nil）
2. 只 RPop 到一人 → 放回（LPUSH），下轮再试，不能丢人
3. 创建房间失败 → 两人都放回

### 原理 3：Pitaya 组件生命周期 Init()

- `func (c *Component) Init()` 组件注册后由框架自动调用；撮合循环在此启动 goroutine
- 与 handler 方法（Join/Cancel）不同：Init 不是路由方法

### 坑 5：mgr 字段的编译依赖

- match.go 的 `mgr *service.RoomManager` 要求 service 包先存在 → 阶段 4 建占位 `RoomManager{}+Create 占位`
- `mgr == nil` 时配对只打日志 `[match] 配对成功 a=x b=y`，阶段 5 传真实实例一行接通

### 坑 6：验收脚本用例顺序（我踩的）

- "单人放回"场景必须**单独用户、队列干净时**测：先 A.join 再 C.join，队列 [A,C] 两人会立刻配对 A+C（FIFO 正确），不是"单人"
- 正确流程：阶段 1 单人排队 2s 无配对 → cancel；阶段 2 A+B 配对

### 阶段 4 实机验收记录（全部通过）

| 用例 | 结果 |
| --- | --- |
| A.join 入队 | code=0 |
| A 重复 join | code=2001（已在匹配中） |
| C 单人排队 2s | 服务日志无配对（放回生效） |
| C.cancel | code=0 |
| A+B 入队 | 服务日志 `[match] 配对成功 a=12 b=13` |

---

## 阶段 5：room 对局核心

### 原理 1：三层结构与锁模型

- room 组件（薄壳，session 取 room_id）→ RoomManager（map+锁+nextID）→ Room（单局状态）
- 每房间一把 `sync.Mutex`：同房间串行、跨房间并行；RoomManager.mu 只保护房间表
- 所有修改房间状态的方法必须先抢 `r.mu`（阶段 6 计时器线程安全的伏笔）

### 原理 2：Place 校验链顺序（错误码从大到小）

`State(RoomState) → seat(RoomNotFound) → turn(NotYourTurn) → 坐标/占位(IllegalMove)` → 落子 → CheckWin → finish / 切回合

- 顺序不能乱：先粗后细，客户端拿到错误码能定位问题层级
- 测试实证：黑在轮到白时重复占位返回 3001（回合先于占位拦截）——符合设计

### 原理 3：resp 复用 push 消息体 + SendPushToUsers

- `pushBoth("room.onPlace", resp)`：resp 与广播字段完全一致，一个结构两用
- `app.SendPushToUsers(route, v, uids, frontType)` 按 uid 定向推送（对应前端 starx.on）
- resp 只回请求者，push 广播全房间——棋类核心消息模型

### 原理 4：finish 的"先清绑定 + 延迟删房"

- `clearRoomID(黑白uid)` 立刻清 session room_id → 玩家马上能再匹配（终局后落子实测返回 4001）
- `removeAfter(id, 10s)` 延迟删房 → 保证双方收完 onGameOver
- 两个动作不能反

### 坑 7：阶段 2 棋盘类型变更的连锁反应

- 学生的 board.go 从 `int8` 改为 `int`（对照 FIVE 后）→ room_mgr.go 里 `color := seat + 1`（int8）与 `Board[x][y]`（int）类型不匹配
- 修复：`color := int(seat) + 1`——棋盘元素与 CheckWin 参数全为 int 时统一走 int

### 阶段 5 实机验收记录（全部通过）

| 验证项 | 结果 |
| --- | --- |
| 撮合 → 双方收 onMatchSuccess（黑 seat=0 / 白 seat=1） | ✅ |
| 开局 onTurn seat=0 黑先手 | ✅ |
| 非己方回合落子 → code=3001 | ✅ |
| 坐标越界 → code=3002 | ✅ |
| 黑对角线五连 → onGameOver winner=0(黑胜) reason=1(五连) moves=9 | ✅ |
| 终局后落子 → code=4001（session 绑定已清） | ✅ |

> 里程碑：阶段 5 结束 = 能完整下完一盘 ✅
