# 五子棋（go Pitaya）学习计划

> 老师：Reasonix ｜ 学生：你 ｜ 目标：从零手写一套可运行的 go Pitaya 五子棋后端，让已就绪的前端跑通全流程。
> 参考：`D:\AI\game\FIVE`（设计稿 + 完整参考实现）；当前工作区 `Five_test` 只有前端 `web/`。

---

## 0. 方案与范围（已确认）

| 项 | 决定 |
| --- | --- |
| 学习方式 | 从零手写 + 老师讲解原理 + 对照 FIVE 参考实现 review 差异 |
| 功能范围 | **最小可玩闭环**：连接 → 登录 → 匹配 → 对弈 → 胜负 → 超时判负 → 重连恢复 |
| 暂不做（后续扩展） | AI 接管、排行榜、对局记录落库、protobuf 正式路径、集群部署 |
| 运行依赖 | Redis：Docker 本地起（`docker-compose`）；MySQL：远程库 `47.97.42.179:3306`（仅登录用） |
| 协议形态 | JSON 序列化（Pitaya `json.NewSerializer()`），消息结构手写 Go struct（路径 A） |
| 部署形态 | Pitaya **Standalone** 单进程，server_type=`room`，WebSocket `:3250`，组件 auth/match/room 共驻 |

**路线图总览**：阶段 0 环境 → 阶段 1 协议/前端 → 阶段 2 棋盘核心（纯 Go）→ 阶段 3 骨架+登录 → 阶段 4 匹配 → 阶段 5 对局核心 → 阶段 6 计时+超时+重连 → 阶段 7 联调收尾。

每个阶段格式统一为四件事：
- 📖 **老师讲**：该阶段要理解的原理（对话中进行，含提问与答疑）
- ✍️ **你来写**：动手任务（尽量不先看 FIVE 的对应实现）
- 🔍 **对照读**：写完后再读 FIVE 对应文件，找差异、说理由
- ✅ **验收**：可运行/可测试的判定标准

---

## 阶段 0：环境准备与 Pitaya 概念铺垫

**目标**：工具链就绪；搞懂 Pitaya 的"组件/路由/session/request-push/序列化"五个核心概念。

- [ ] Docker 拉起本地 Redis：`docker compose up -d redis`，`redis-cli ping` 返回 PONG
- [ ] 确认远程 MySQL 连通（`gomoku` 库与 `schema.sql` 已建表，凭据来自开发文档 §2）
- [ ] `go version` ≥ 1.25（本机 1.26.5 ✓）；在 `Five_test` 初始化 `go mod init five`
- [ ] 读 `FIVE/开发文档.md` §3（架构）、§6（组件与路由约定）、§7（协议与客户端接入）
- [ ] 对照 FIVE 的 `deploy/docker-compose.yml` 理解本地依赖编排

📖 讲：Pitaya 中 frontend/gate 与 backend 的差别、component 注册与路由映射规则（`component.WithName` + `strings.ToLower`）、session 的 uid 绑定与数据存取、request 与 push 的流向、为什么联调期用 JSON 序列化。

🔍 对照读：`FIVE/cmd/server/main.go`（builder 组装流程，先看不动手）。

✅ 验收：`go build ./...` 能过（空模块即可）；Redis PONG；能口述"客户端一条 `auth.login` 消息到服务器返回，中间经过了哪些环节"。

---

## 阶段 1：读懂前端与协议（客户端已就绪）

**目标**：把 `web/index.html` 的交互流程和 `gomoku.proto` 的协议全部映射清楚——这是后端要实现的服务契约。

- [ ] 读 `web/index.html`：按钮状态机（setButtons）、starx 生命周期、每个 `starx.request` 和 `starx.on` 的对应关系
- [ ] 画一条"客户端视角时序图"：连接 → 登录 → 匹配 → 撮合成功 → 落子 → 终局
- [ ] 对照 `docs/gomoku.proto` 与 `internal/model/messages.go`，列出路由表：

| 方向 | 路由 | req | resp / push |
| --- | --- | --- | --- |
| req/resp | `auth.login` | C2SLogin | S2CLogin |
| req/resp | `match.join` / `match.cancel` | C2SJoinMatch / C2SCancelMatch | S2CJoinMatch / S2CCancelMatch |
| push | 撮合成功 | — | `room.onMatchSuccess` |
| req/resp | `room.place` / `room.leave` / `room.reconnect` | C2SPlacePiece / C2SLeaveGame / C2SReconnect | S2CPlacePiece / S2CLeaveGame / S2CReconnect |
| push | 回合/计时/落子/终局 | — | `room.onTurn` / `room.onTimer` / `room.onPlace` / `room.onGameOver` |

- [ ] 记住错误码表（proto 文件末尾）与 `internal/common/errors.go` 的常量一一对应

📖 讲：snake_case JSON 字段与 proto 的映射；Pitaya 路由两段式（`auth.login`）在 Standalone 模式下的含义；为什么 resp 与 push 共用消息结构。

✅ 验收：不看代码能画出前端完整时序图，能说出每个按钮触发哪个路由、每个 push 前端如何处理。

---

## 阶段 2：棋盘核心逻辑（纯 Go，先不碰框架）

**目标**：写出正确的棋盘数据结构和五连判定，这是全部对局逻辑的地基，且完全可单测。

- [x] 定义 `internal/common/board.go`：`type Board [15][15]int8`（0=空 1=黑 2=白）、`BoardSize`/`WinCount` 常量
- [x] 实现 `CheckWin(b, x, y, color)`：沿横/竖/两斜共 4 个方向双向计数，≥5 即胜
- [x] 边界测试：五连、活四/冲四（非五连）、贴边、角落、对角线
- [x] 写 `board_test.go` 覆盖关键用例，`go test ./internal/common/` 全绿

🔍 对照读：`FIVE/internal/common/board.go`（很短，比对方向数组写法与边界处理）。

✅ 验收：`go test -v ./internal/common/` 通过；能向老师解释"为什么每落一子只检查该子所在四条线即可判胜"。

---

## 阶段 3：项目骨架 + auth 登录组件

**目标**：跑起第一个可连接的 Pitaya 服务，并完成登录（MySQL + bcrypt + token + session 绑定）。

- [x] `config/config.yaml`（port、mysql、redis、game 参数）与 `internal/model/config.go`
- [x] `cmd/server/main.go`：builder 组装（Standalone、WS acceptor、JSON serializer）、组件注册、MySQL/Redis 连接（失败不阻塞，nil 降级）
- [x] `internal/model/mysql.go`：`GetUserByUsername` / `CreateUser` / `GetUserStats`（对照 `docs/schema.sql`）
- [x] `internal/app/auth/auth.go`：`auth.login` —— 校验 → 查库 → 不存在则注册（bcrypt 存密码）→ 生成 token → Redis 写 session/在线状态 → `s.Bind(uid)` → 返回 S2CLogin
- [x] `internal/model/redis.go`：`SetSession`/`SetOnline`/`GetSession`

📖 讲：bcrypt 为什么不能明文存密码；token 与 session 的关系（token 存 Redis，uid↔连接绑定在 Pitaya session）；"登录即注册"的并发安全写法（唯一键冲突回查）；组件 nil 降级的设计意图。

🔍 对照读：`FIVE/internal/app/auth/auth.go`、`internal/model/mysql.go`、`internal/model/redis.go`。

✅ 验收：浏览器打开 `web/index.html` → 连接 `127.0.0.1:3250` → 登录成功日志出现 `uid/nickname/elo`；重复登录同一账号两次创建新用户且密码正确性校验生效。

---

## 阶段 4：match 匹配组件（Redis 队列撮合）

**目标**：实现 `match.join` / `match.cancel` 与后台撮合循环，撮合成功即创建房间并推送 `room.onMatchSuccess`。

- [x] `internal/model/redis.go` 增加：`MatchEnqueue`(LPUSH) / `MatchRemove` / 队列 key `match:queue`
- [x] `internal/app/match/match.go`：`Join`（未登录拒绝、去重、入队）、`Cancel`（出队）、`Init` 启动撮合循环
- [x] 撮合循环：每 500ms `RPop` 取两名 → FIFO 先到先配 → 交给 RoomManager（阶段 5 先留接口）
- [x] 边界：队列只有一人时放回、撮合失败回队列不丢人

📖 讲：为什么用 Redis 列表做队列（原子弹出保证同一对手不被两个撮合循环抢到）；FIFO 先到先配 vs 按 elo 撮合；`LPUSH`+`RPop` 的方向约定。

🔍 对照读：`FIVE/internal/app/match/match.go`。

✅ 验收：开两个浏览器标签（player1 / player2）→ 分别登录 → 都点"进入匹配" → 双方在 1 秒内收到 `onMatchSuccess` 与自己的 seat/颜色。

---

## 阶段 5：room 对局核心（落子 → 判胜 → 终局广播）

**目标**：实现房间管理器与 `room.place`，完成"落子校验 → 更新棋盘 → 五连判定 → 切换回合 → 广播"，这是整个项目的核心。

- [x] `internal/service/room_mgr.go`：`RoomManager`（内存 map + 锁 + nextID）、`Room` 结构（Seat/TurnSeat/Board/Moves/State）、`Create`（初始化、写双方 session `room_id`、分别推送 onMatchSuccess、推开局 onTurn）
- [x] `Room.Place(uid,x,y)`：校验（房间状态/座位归属/回合/坐标/占位）→ 落子 → `CheckWin` → 赢则 `finish` 广播 `onGameOver`；否则切回合推 `onTurn`
- [x] `Room.finish`：标记 RoomOver、广播终局、清 session 绑定、延迟 10s 删房间
- [x] `internal/app/room/room.go`：`room.place` 组件方法（从 session 取 room_id → mgr.Place）
- [x] `internal/common/consts.go` / `errors.go`：座位、终局原因、房间状态、错误码

📖 讲：每房间一把 `sync.Mutex` 防并发落子；为什么 resp 直接复用 push 的消息体；终局后"清 session 绑定 + 延迟删房"让玩家可立即进下一局；`SendPushToUsers` 按 uid 定向推送（对应前端 `starx.on('room.onPlace')`）。

🔍 对照读：`FIVE/internal/service/room_mgr.go`、`internal/app/room/room.go`。

✅ 验收：双开页面匹配成功后对弈，黑先白后轮流落子，五连出现后双方弹窗"胜利/失败"，日志含回放 moves；非法落子（重复占位、非己方回合）被拒绝且带正确错误码。

---

## 阶段 6：限时计时 + 超时判负 + 重连恢复

**目标**：服务器权威计时（`time.AfterFunc` + 每秒 `onTimer` 推送），超时判负；`room.reconnect` 全量恢复棋盘与计时。

- [ ] `Room.startTurn` / `onTimeout` / `pushTicker`：回合切换时启动超时定时器；倒计时推送（剩余 ≤5s 时加密到 200ms）；超时 `finish(对方胜, EndTimeout)`
- [ ] `room.leave`：主动离开判负（`EndLeave`）
- [ ] `Room.Snapshot` + `room.reconnect`：token 校验（Redis session）→ 定位房间 → 校验玩家在房 → 新连接 `s.Bind` + 写 room_id → 返回全量状态 → 推 `onPlayerBack`
- [ ] 计时线程安全：ticker 与 Place/finish 共享 `r.mu`

📖 讲：为什么服务器权威计时（客户端时间不可信）；`time.AfterFunc` 回调里必须重新抢锁再判断 `State`；ticker 收 `pushStop` 优雅退出；重连时旧 session 可能未清理导致的 Bind 冲突为何可忽略（房间定位已由 room_id 保证）。

🔍 对照读：FIVE `room_mgr.go` 中 `startTurn`/`pushTicker`/`Snapshot` 与 `room.go` 中 `Reconnect`。

✅ 验收：① 一方不落子，倒计时到 0 判负；② 对局中刷新页面 → 自动恢复棋盘/回合/剩余时间；③ 离开对局按钮生效并判负；④ 终局后点"进入下局"能重新匹配下一盘。

---

## 阶段 7：联调验证与收尾复盘

**目标**：全流程 + 边界场景手工验证，收尾整理。

- [ ] 全流程走查：连接 → 登录 → 匹配 → 完整对弈 → 终局弹窗 → 下一局（连续 2 盘以上）
- [ ] 边界清单逐项验证：重复点击落子、坐标越界（前端已挡，验证后端仍拒绝）、匹配中重复 join、未登录直接 place、终局后再落子
- [ ] `go vet ./...`、`go test ./...` 全绿；随手清理临时文件
- [ ] 复盘：与 FIVE 参考实现的差异清单（哪些地方你写得不同/更简/更好）；`FIVE` 中尚未实现的能力（AI 接管、结算落库、排行榜、protobuf 路径、集群）分别对应你后续哪些扩展阶段

📖 讲：服务端权威校验的价值（防前端绕过）；日志与错误码的规范；后续扩展路线怎么加（AI 组件独立成 `internal/ai`、结算写 `game_record`、rank 组件、protobuf 生成代码切换）。

✅ 验收：**你**能不看任何参考代码，独立从零把本项目 `go build && go run ./cmd/server -c config/config.yaml` 跑起来并完成双人全流程演示；能向老师讲清每一层（组件 → service → common）的职责划分。

---

## 学习纪律（老师的要求）

1. **先想再写**：每个阶段先听讲解、先画/写设计再动代码；卡住 15 分钟就问，不要先翻 FIVE。
2. **写完再对照**：对照 FIVE 后必须说出至少一条"你的实现与参考的差异及理由"。
3. **验收不过不进下一阶段**：每阶段 ✅ 验收通过才推进。
4. **问题归档**：所有答疑沉淀为 `学习笔记.md`（约定俗成，与计划同目录）。

> 里程碑：阶段 3 结束 = 你的第一个"能连、能登录"的 Pitaya 服务；阶段 5 结束 = 可完整下完一盘；阶段 6 结束 = 达到"最小可玩闭环"交付标准。
